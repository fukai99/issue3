<div class="col-md-3">
 <h2>User info</h2>
  <%= image_tag user.use_id.get_profile_image(100,100) %>
<table class="table">
  <tbody>
    <tr></tr>
    <tr>
      <th>name</th>
      <th><%= user.name %></th>
    </tr>

    <tr>
      <th>introduction</th>
      <th><%= user.introduction %></th>
    </tr>
  </tbody>
</table>

<div class="row">

<%= link_to root_path ,class:"btn btn-success btn-block" do%>
<i class="fa-solid fa-user-gear"></i>
<% end %>
</div>

<h1>New book</h1>
<%= form_with model: book do |f| %>
<h4>Title</h4>
  <%= f.text_field :title %>
	
  <h4>Opinion</h4>
  <%= f.text_area :body %>
	
  <%= f.submit 'Create Book' %>
<% end %>

</div>

<!--
<div class="col-md-8 offset-md-1">
  <h2>Book detail</h2>
  <table class="table table-hover table-inverse">
    <% @books.each do |book| %>
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= image_tag book.user.get_profile_image(100,100) %></td>
        <td><%= link_to book_path(book.user) do %>
        <%= book.title %></td>
        <td><%= book.body %></td>
      </tr>
    </tbody>
     <% end %>
     </table>
    </div>
    -->
    
    
    
    
    <h2>Book detail</h2>
    <%= link_to user(user_id) do %>
    <%= user.name %>
    <p><%= usre.</p>