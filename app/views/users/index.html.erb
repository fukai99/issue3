<%= render 'show', user: @user, book: @book %>

<h2>Users</h2>
<% @users.each do |user| %>

<p>image</p>

<p>name</p>
<%= image_tag user.get_profile_image(100,100) %>
<span><%= user.name %></span>
<%= link_to "Show", user_path(user) %>
  
<% end %>